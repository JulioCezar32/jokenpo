import playermove

class match():


    def winner_loser(self):
        self.player_
        if (self.'Paper' && 'Stone'):
            self.winner = 'Paper'
            self.loser = 'Stone'
            return self

        if ('Scissor' && 'Paper'):
            self.winner = 'Scissor'
            self.loser = 'Paper'

        if ( 'Stone' &&'Scissor'):
            self.winner = 'Stone'
            self.loser = 'Scissor'
